@model IEnumerable<WebApp_LMIGREATICE.Models.projectCountry>

@{
    ViewBag.Title = "Index";
}

<h2 class="text-center">List of LMI GREAT ICE countries</h2>

<p>
    @Html.ActionLink("Add country", "Create", "projectCountry", new { @class = "btn btn-defualt wine-button" })
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.nameCountry)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.idProjectCountry)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.nameCountry)
            </td>
            @*<td>
                @Html.ActionLink("Details", "Details", new { id = item.idProjectCountry })
            </td>*@
        </tr>
    }

</table>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}